extends layout-auth

block content
      .container
      br
      br
      br
      .section
      main
        center
          .section
          .container
            .z-depth-1.grey.lighten-4.row(style='display: inline-block; padding: 32px 48px 0px 48px; border: 1px solid #EEE;')
              h5.orange-text Autenticación de Usuario
              form(class='form-signin', action='/login', method='post')
                  input#email.validate(type='text', name='email', class='form-control', placeholder='Correo electrónico',required)
                  input#password.validate(type='password', name='password', class='form-control nomargin', placeholder='Contraseña', required)
                  br
                  button(class='btn btn-lg btn-primary btn-block', type='submit', name='btn_login') Ingresar
                  label(style='float: right;')                  
          .container
            a(href='/', class='text-center') Volver
      #message
      if message
      h1.text-center.error-message #{message}

      // jQuery
      script(src='vendor/jquery/jquery.min.js')
